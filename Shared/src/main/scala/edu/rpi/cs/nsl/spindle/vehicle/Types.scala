package edu.rpi.cs.nsl.spindle.vehicle

object Types {
  type NodeId = Int // UID for vehicle in database
  type Timestamp = Long
  //type CacheMap = Map[CacheTypes.Value, TSCache[_]] //TODO: determine how to deal with this
}